fun main() {

    val active = true

    // if and else statements
    if (active)
    {
        println("The system is active and ready.")
    }
    else
    {
        println("Error. System cannot engage.")
    }

    // when statement

}